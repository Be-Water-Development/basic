function getRandomInt(max) {
  return Math.floor(Math.random() * max);
}

const rouletteBoard = () => {
  const hitHistory = []
  const spinIt = (luckyNumber) => {
    // Directions: fill this function out... 
    // this function should generate a random number between 0 and 100 and push it to 
    // hitHistory.
    // This function should time the amount of time it takes to hit the luckyNumber (param)
    // and continuing executing 'spins' with generated numbers until the luckyNumber is hit
    // This function should return out the hitHistory and the time it took to hit the luckyNumber
  }
}


